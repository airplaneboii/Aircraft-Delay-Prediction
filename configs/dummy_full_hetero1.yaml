# Dummy model example (full-batch, no neighbor sampling)
# Leave `data_path` blank to rely on auto-selection or pass `--data_path` on CLI
mode: train
rows: null
graph_type: hetero1
save_graph: ""
load_graph: "hetero1_12m.pt"
model_type: dummy
data_path: ""
graph_dir: src/graph/
model_dir: src/models/
model_file: dummy_hetero1_12m
epochs: 50
lr: 0.001
prediction_type: regression
neighbor_sampling: false
neighbor_fanouts: ""
verbosity: 1

# Notes:
# - This config demonstrates running the lightweight `dummy` on the full graph (no sampling).
# - Use `python main.py -c configs/dummy_nosample.yaml --data_path path/to/your.csv` to run.
